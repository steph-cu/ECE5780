project Default is

   package Compiler is
      for Switches ("ada") use ("-g", "-O2");
   end Compiler;

   for Main use ("part1_skeleton.adb", "helloworld.adb") & project'Main;

end Default;

